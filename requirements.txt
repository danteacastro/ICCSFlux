# ICCSFlux Development Dependencies
# Install with: pip install -r requirements.txt

# ===========================================
# CORE (all services)
# ===========================================
paho-mqtt>=2.0.0
cryptography>=42.0.0  # TLS certificate generation (first-run auto-setup)

# ===========================================
# DAQ SERVICE
# ===========================================
bcrypt>=4.0.0            # Password hashing (user_session.py)
numpy>=1.26.0
scipy>=1.10.0
pymodbus>=3.0.0
opcua>=0.98.0
pyserial>=3.5
requests>=2.28.0
psutil>=5.9.0
# nidaqmx - REQUIRED for hardware I/O. Not pip-installable.
#   Install NI-DAQmx driver from https://www.ni.com/en/support/downloads/drivers/download.ni-daq-mx.html
#   Then: pip install nidaqmx (Python bindings, requires driver to be installed first)

# ===========================================
# OPTO22 NODE
# ===========================================
# requests (listed above)
# numpy, scipy (listed above)

# ===========================================
# CRIO NODE
# ===========================================
# nidaqmx - see DAQ SERVICE section above
# numpy, scipy (listed above)

# ===========================================
# DATABASE (optional)
# ===========================================
psycopg2-binary>=2.9.0  # PostgreSQL (no server install needed, client-only)

# ===========================================
# BUILD
# ===========================================
pyinstaller>=6.0.0

# ===========================================
# TESTING
# ===========================================
pytest>=8.0.0
pytest-asyncio>=0.23.0
