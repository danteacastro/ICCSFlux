listener 1883 0.0.0.0
protocol mqtt

listener 9002 127.0.0.1
protocol websockets

allow_anonymous true

# Logging - errors only, no file (prevents disk bloat)
log_dest stdout
log_type error
log_type warning
connection_messages false
log_timestamp true
